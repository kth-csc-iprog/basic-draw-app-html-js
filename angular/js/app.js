// We initialize the Angular application (module) and 
// store it in the global variable (window.app) so we 
// can have it available when initializing controllers
window.app = angular.module("basicDraw",[]);
